import Foundation
import ImageIO
import CoreGraphics

/// Loads stereo image pairs from various file formats.
/// All loading happens off the main thread via async/await.
enum ImageLoader {
    // Placeholder — implementation in Phase 2
    static func load(from url: URL) async throws -> StereoImage {
        fatalError("Not yet implemented")
    }
}
